import { CSSProperties } from 'react'
import { STYLE_BORDER_RADIUS } from '../../../constants/styles'

export const STYLE_CONTEXT_MENU: CSSProperties = {
  borderRadius: STYLE_BORDER_RADIUS,
  fontSize: 16,
}
